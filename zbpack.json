{
  "build_command": "node scripts/generate-manifest.js && tsc -b && tsc -p tsconfig.server.json && vite build",
  "start_command": "node dist-server/index.js",
  "environment": {
    "NODE_ENV": "production"
  }
}

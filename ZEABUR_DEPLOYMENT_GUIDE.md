/**
 * Zeabur 部署操作指南
 * 更新時間：2026-02-05 18:30
 * 更新者：AI Assistant
 * 更新摘要：詳細的 Zeabur 部署步驟說明
 */

# 🚀 Zeabur 部署操作指南

**專案**: Second Brain Web  
**GitHub**: https://github.com/smlht2005/SecondBrainWeb  
**最新 Commit**: fb52fc2 (Production deployment preparation and CORS fix)

---

## ✅ 步驟 1：確認 Git 推送狀態

已完成 ✅：
```
✅ 所有變更已提交到 Git
✅ 已推送到 GitHub (main 分支)
✅ Commit hash: fb52fc2
```

---

## 📋 步驟 2：登入 Zeabur 並創建專案

### 2.1 登入 Zeabur

1. 訪問 [https://zeabur.com](https://zeabur.com)
2. 使用 GitHub 帳號登入

### 2.2 創建新專案

1. 點擊右上角 **New Project** 按鈕
2. 選擇 **Deploy New Service**
3. 選擇 **Git** 作為來源

### 2.3 連接 GitHub Repository

1. 點擊 **Configure GitHub**（如果是第一次使用）
2. 授權 Zeabur 訪問您的 GitHub 帳號
3. 選擇 Repository: **smlht2005/SecondBrainWeb**
4. 選擇分支: **main**
5. 點擊 **Deploy**

---

## ⚙️ 步驟 3：配置環境變數

在 Zeabur Dashboard 中：

1. 找到剛創建的服務（SecondBrainWeb）
2. 點擊服務名稱進入詳情頁
3. 選擇左側 **Variables** 標籤
4. 添加以下環境變數：

| 變數名稱 | 值 | 說明 |
|---------|-----|------|
| `NODE_ENV` | `production` | 生產環境標識 |

**重要**: `PORT` 變數由 Zeabur 自動注入，無需手動設定。

### 截圖參考

```
┌─────────────────────────────────────┐
│ Environment Variables               │
├─────────────────────────────────────┤
│ NODE_ENV = production               │
└─────────────────────────────────────┘
```

---

## 💾 步驟 4：配置 Volume（重要！）

### 4.1 為什麼需要 Volume？

- 本地 `brain/` 和 `memory/` 目錄的測試資料不會部署到生產環境
- Volume 提供持久化存儲，確保資料不會在容器重啟後丟失
- 這是生產環境資料的唯一存儲位置

### 4.2 配置步驟

1. 在服務詳情頁，選擇左側 **Volumes** 標籤
2. 點擊 **Add Volume** 按鈕
3. 填寫以下資訊：

| 欄位 | 值 | 說明 |
|------|-----|------|
| **Volume Name** | `secondbrain-data` | 自訂名稱 |
| **Mount Path** | `/home/node/.openclaw/workspace` | 必須與程式碼中的路徑一致 |
| **Size** | `1 GB` | 可根據需求調整 |

4. 點擊 **Create** 確認

### 截圖參考

```
┌─────────────────────────────────────────────────┐
│ Volume Configuration                            │
├─────────────────────────────────────────────────┤
│ Name:       secondbrain-data                    │
│ Mount Path: /home/node/.openclaw/workspace     │
│ Size:       1 GB                                │
│ Status:     Active                              │
└─────────────────────────────────────────────────┘
```

---

## 🔄 步驟 5：等待部署完成

### 5.1 監控建置過程

1. 回到服務詳情頁的 **Logs** 標籤
2. 觀察建置日誌，確認沒有錯誤

### 5.2 預期建置流程

```bash
# 1. 拉取 GitHub 代碼
Cloning repository...

# 2. 檢測到 Dockerfile
Detected Dockerfile, using Docker build...

# 3. 執行 Docker 建置
Step 1/12 : FROM node:20-alpine AS builder
Step 2/12 : WORKDIR /app
...
Step 12/12 : CMD ["npm", "start"]

# 4. 部署成功
Deployment successful!
Service URL: https://secondbrainweb-xxxx.zeabur.app
```

### 5.3 建置時間

預計 **2-4 分鐘**，取決於：
- 網路速度
- 依賴安裝時間
- Docker 層快取

---

## 🌐 步驟 6：獲取服務 URL

部署成功後，Zeabur 會自動分配一個 URL：

1. 在服務詳情頁頂部找到 **Domain** 欄位
2. 複製 URL（格式：`https://secondbrainweb-xxxx.zeabur.app`）
3. 在瀏覽器中訪問此 URL

### 預設網域格式

```
https://<service-name>-<random-id>.zeabur.app
```

例如：
```
https://secondbrainweb-abc123.zeabur.app
```

---

## ✅ 步驟 7：部署後驗證

### 7.1 健康檢查

在瀏覽器或終端中測試：

```bash
# 替換為您的實際 URL
curl https://your-app.zeabur.app/health
```

**預期回應**：
```json
{
  "status": "healthy",
  "uptime": 123.45,
  "timestamp": "2026-02-05T10:30:00.000Z",
  "storage": {
    "brain": true,
    "memory": true
  }
}
```

### 7.2 API 端點測試

```bash
# 測試 Brain API
curl https://your-app.zeabur.app/api/brain/files

# 測試 Memory API
curl https://your-app.zeabur.app/api/memory/logs
```

**預期回應**（驗證資料已包含在 Docker 映像中）：
```json
[
  {
    "name": "測試文件",
    "fileName": "測試文件.md",
    "type": "brain"
  }
]
```

或對於 memory：
```json
[
  {
    "date": "2026-02-05",
    "fileName": "2026-02-05.md",
    "type": "memory"
  }
]
```

**注意**：驗證資料（`brain/測試文件.md` 和 `memory/2026-02-05.md`）已包含在 Docker 映像中，部署後可立即看到，無需手動上傳。

### 7.3 前端頁面測試

1. 在瀏覽器訪問：`https://your-app.zeabur.app`
2. 打開開發者工具（F12）
3. 檢查：
   - ✅ 頁面是否正常載入
   - ✅ Console 無錯誤訊息
   - ✅ Network 標籤中 CSS 和 JS 正確載入
   - ✅ 靜態資源的 MIME type 正確（`text/css`, `text/javascript`）

### 7.4 靜態資源驗證

在 Network 標籤中檢查：

| 文件 | 狀態碼 | Content-Type | 大小 |
|------|--------|--------------|------|
| `/` | 200 | `text/html` | ~2 KB |
| `/assets/index-*.css` | 200 | `text/css` | ~900 bytes |
| `/assets/index-*.js` | 200 | `text/javascript` | ~150 KB |
| `/vite.svg` | 200 | `image/svg+xml` | ~1 KB |

**如果出現 404 或 MIME type 錯誤，請參考「故障排除」章節。**

---

## 📁 步驟 8：上傳額外生產資料（可選）

**重要說明**：驗證資料（`brain/測試文件.md` 和 `memory/2026-02-05.md`）已包含在 Docker 映像中，部署後可立即看到。此步驟僅適用於需要上傳額外生產資料的情況。

### 方法 A：使用 Zeabur CLI

```bash
# 1. 安裝 Zeabur CLI
npm install -g @zeabur/cli

# 2. 登入
zeabur login

# 3. 列出您的服務
zeabur service list

# 4. 準備額外生產資料（驗證資料已包含在映像中）
mkdir production-data
cd production-data
mkdir brain memory

# 5. 建立額外文件（驗證資料已存在，這是額外的）
echo "# 歡迎使用 Second Brain" > brain/welcome.md
echo "# $(date +%Y-%m-%d) 系統上線" > memory/$(date +%Y-%m-%d).md

# 6. 上傳到 Volume（與映像中的驗證資料共存）
zeabur volume upload \
  --service <your-service-id> \
  --volume secondbrain-data \
  --local-path ./brain/welcome.md \
  --remote-path /home/node/.openclaw/workspace/brain/welcome.md
```

### 方法 B：使用 Zeabur Web 終端

1. 在 Zeabur Dashboard → 服務詳情 → **Terminal** 標籤
2. 進入容器終端
3. 手動建立文件：

```bash
cd /home/node/.openclaw/workspace

# 建立目錄（如果不存在）
mkdir -p brain memory

# 建立額外文件（驗證資料已存在於映像中）
cat > brain/welcome.md << EOF
# 歡迎使用 Second Brain

這是您的知識管理系統。

## 功能特點

- 📝 Markdown 支援
- 🏷️ 標籤分類
- 🔍 全文搜索
- 📅 時間軸記憶
EOF

cat > memory/2026-02-05.md << EOF
# 2026-02-05 系統上線

## 今日記事

- 系統成功部署到 Zeabur
- 完成初始配置
EOF
```

### 方法 C：使用 API 上傳（需要自行實作）

如需批量上傳或定期同步，建議實作檔案上傳 API。

---

## 🔒 步驟 9：更新 CORS 配置（如果使用自訂網域）

### 9.1 如果使用 Zeabur 預設網域

無需修改，CORS 配置已經正確。

### 9.2 如果綁定自訂網域

1. 在 Zeabur Dashboard → Domains → **Add Custom Domain**
2. 輸入您的網域（例如：`clawbrain.yourdomain.com`）
3. 按照指示設定 DNS CNAME 記錄
4. 更新 `server/index.ts` 中的 CORS 白名單：

```typescript
const allowedOrigins = [
    'https://clawbrain.zeabur.app',
    'https://clawbrain.yourdomain.com'  // 新增自訂網域
];
```

5. 提交變更並推送到 GitHub
6. Zeabur 會自動重新部署

---

## 🔍 故障排除

### 問題 1：部署失敗，出現 "Build failed"

**可能原因**：
- Dockerfile 語法錯誤
- 依賴安裝失敗

**解決方案**：
1. 檢查 Zeabur 的 Logs 標籤，查看詳細錯誤訊息
2. 確認 Dockerfile 與本地測試時一致
3. 確認 `package.json` 中的 dependencies 正確

### 問題 2：靜態資源返回 404 或 MIME type 錯誤

**症狀**：
```
Refused to apply style from '...' because its MIME type ('text/html') 
is not a supported stylesheet MIME type
```

**原因**：
- 靜態文件未正確建置
- `dist/` 目錄缺失

**解決方案**：
1. 確認 `Dockerfile` 中的 `RUN npm run build` 執行成功
2. 檢查建置日誌，確認 Vite 建置完成
3. 確認 `server/index.ts` 中的 `distPath` 路徑正確

### 問題 3：API 返回空陣列

**原因**：如果 API 返回空陣列，可能是：
- Docker 映像中沒有包含驗證資料（檢查 `.dockerignore` 是否排除了 `brain/` 和 `memory/`）
- Volume 掛載路徑不正確

**解決方案**：
- 確認 `.dockerignore` 沒有排除 `brain/` 和 `memory/` 目錄
- 確認驗證資料已提交到 Git（`brain/測試文件.md` 和 `memory/2026-02-05.md`）
- 確認 Volume 已正確配置（如果使用 Volume 存儲額外資料）
- 重新建置 Docker 映像並部署

### 問題 4：CORS 錯誤

**症狀**：
```
Access to fetch at '...' from origin '...' has been blocked by CORS policy
```

**解決方案**：
1. 確認環境變數 `NODE_ENV=production` 已設定
2. 檢查 `server/index.ts` 中的 `allowedOrigins` 是否包含您的網域
3. 如果使用自訂網域，參考「步驟 9」更新 CORS 配置

### 問題 5：容器啟動失敗

**可能原因**：
- 環境變數缺失
- Volume 掛載失敗

**解決方案**：
1. 檢查 Zeabur Logs 標籤中的錯誤訊息
2. 確認 Volume 已正確配置
3. 確認 `NODE_ENV` 環境變數已設定

---

## 📊 部署後監控

### 監控項目

| 項目 | 檢查方式 | 正常範圍 |
|------|---------|---------|
| **CPU 使用率** | Zeabur Dashboard → Metrics | < 50% |
| **記憶體使用** | Zeabur Dashboard → Metrics | < 256 MB |
| **請求數量** | Zeabur Dashboard → Metrics | - |
| **回應時間** | Zeabur Dashboard → Metrics | < 200ms |
| **健康狀態** | `/health` 端點 | `"status": "healthy"` |

### 定期檢查

建議每日檢查：
```bash
# 健康檢查
curl https://your-app.zeabur.app/health

# API 測試
curl https://your-app.zeabur.app/api/brain/files
curl https://your-app.zeabur.app/api/memory/logs
```

---

## 🎯 部署檢查清單

請逐一確認：

### 部署前
- [x] 所有變更已提交到 Git
- [x] 已推送到 GitHub
- [x] Dockerfile 已建立並測試
- [x] `.dockerignore` 已配置
- [x] `.env.production` 已建立
- [x] 本地 Docker 測試通過

### 部署中
- [ ] Zeabur 專案已創建
- [ ] GitHub Repository 已連接
- [ ] 環境變數已設定（`NODE_ENV=production`）
- [ ] Volume 已配置（`/home/node/.openclaw/workspace`）
- [ ] 建置成功，無錯誤

### 部署後
- [ ] 服務 URL 可訪問
- [ ] 健康檢查端點返回 `200 OK`
- [ ] API 端點正常運作
- [ ] 前端頁面正常載入
- [ ] 靜態資源（CSS/JS）正確載入
- [ ] Console 無 CORS 錯誤
- [ ] MIME type 正確
- [ ] （可選）生產資料已上傳

---

## 📝 重要提醒

1. **資料隔離**
   - ⚠️ 本地 `brain/` 和 `memory/` 目錄的測試資料不會被部署
   - ✅ 生產環境使用 Zeabur Volume 進行資料持久化
   - ✅ `.gitignore` 和 `.dockerignore` 已配置排除測試資料

2. **Volume 備份**
   - 建議定期備份 Volume 資料
   - 可使用 Zeabur CLI 或手動下載

3. **環境變數**
   - `NODE_ENV=production` 必須設定
   - `PORT` 由 Zeabur 自動注入

4. **CORS 配置**
   - 預設允許 `https://clawbrain.zeabur.app`
   - 如使用自訂網域，需更新 `server/index.ts`

5. **成本控制**
   - Zeabur 提供免費額度
   - 超出額度會產生費用
   - 建議監控資源使用情況

---

## 🎉 恭喜！

如果所有步驟都完成且驗證通過，您的 Second Brain Web 應用現在已成功部署到 Zeabur！

**您的應用 URL**: `https://<your-service>.zeabur.app`

### 下一步建議

1. 上傳初始知識庫內容
2. 設定自訂網域（可選）
3. 配置 CI/CD 自動部署（已透過 GitHub 整合）
4. 定期監控應用健康狀態
5. 備份重要資料

---

**更新時間**: 2026-02-05 18:30  
**文檔版本**: 1.0  
**維護者**: AI Assistant

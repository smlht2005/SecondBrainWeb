# Zeabur 配置 - 使用 Dockerfile 部署
# 更新時間：2026-02-17
# 更新者：AI Assistant
# 更新摘要：切換到 Dockerfile 部署模式

build:
  # 使用 Dockerfile 進行建置
  dockerfile: Dockerfile.prod

deploy:
  # 不需要額外命令，Dockerfile 會處理
  start_command: (none)

# 注意：Zeabur 會自動檢測並使用 Dockerfile

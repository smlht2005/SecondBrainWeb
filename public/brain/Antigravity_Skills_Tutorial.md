# Antigravity Agent Skills: 從入門到實戰 (學習筆記)

**核心概念**: Skill 是 Antigravity (Google 的 Agentic 平台) 的擴充模組。它解決了 LLM "Context Saturation" (上下文飽和) 的問題，讓 Agent 不用一次載入所有知識，而是「按需加載 (Progressive Disclosure)」。

## 📌 為什麼需要 Skills?
1.  **避免 Tool Bloat (工具臃腫)**: 不要在對話一開始就塞入幾百個 MCP Tools (如 GitHub, Playwright)，這會浪費 Token 並讓模型困惑。
2.  **專業分工 (Specialization)**: 將特定的工作流程 (如「資料庫遷移」、「代碼審查」) 封裝成 Skill，讓通用的 Gemini 模型瞬間變成該領域的專家。
3.  **降低延遲與成本**: 只有當使用者意圖匹配時，才載入該 Skill 的詳細指令與腳本。

---

## 🏗️ Skill 的四大模式 (Patterns)

### 1. Basic Router Pattern (基礎路由)
*   **結構**: 只有 `SKILL.md`。
*   **用途**: 純文字指令，告訴 Agent "遇到 X 情況該怎麼做"。
*   **範例**: "當使用者問及公司請假流程時，請回答：先填單 -> 主管簽核..."

### 2. The Reference Pattern (參考資料)
*   **結構**: `SKILL.md` + `/references` 資料夾。
*   **用途**: 讓 Agent 讀取特定的文件或規範。
*   **範例**: "寫 Python 時請參考 `/references/style_guide.md` 的 PEP8 規範"。

### 3. The Few-Shot Pattern (範例教學)
*   **結構**: `SKILL.md` + `/examples` 資料夾。
*   **用途**: 提供 "Input -> Output" 的範例，讓 Agent 模仿風格。
*   **範例**: "產生 Git Commit Message 時，請參考 `/examples/good_commits.txt` 的格式"。

### 4. The Tool Use Pattern (工具整合)
*   **結構**: `SKILL.md` + `/scripts` 資料夾。
*   **用途**: 賦予 Agent 執行腳本的能力。
*   **範例**: "當使用者說 '部署' 時，執行 `/scripts/deploy.sh`"。

---

## 🚀 實戰：如何建立一個 Skill?

**步驟 1**: 在 `~/.antigravity/skills/` (或專案目錄) 下建立資料夾，例如 `git-commit-helper`。

**步驟 2**: 撰寫 `SKILL.md` (這是 Skill 的靈魂)。
```markdown
---
name: git-commit-helper
description: 協助產生符合 Conventional Commits 規範的 Git 提交訊息。
---

# Git Commit Helper

當使用者要求 "幫我 Commit" 時，請遵循以下流程：
1. 執行 `git diff --cached` 查看變更。
2. 分析變更內容，決定 Type (feat, fix, docs...)。
3. 生成訊息格式：`<type>: <description>`。
4. 使用 `git commit -m "..."` 執行提交。
```

**步驟 3**: (可選) 加入 `/scripts/validate_commit.py` 來檢查格式。

---

## 💡 關鍵心法
*   **Skill 是 "動態" 的**: Agent 會根據 Prompt 自動判斷要掛載哪個 Skill。
*   **Prompt Engineering**: `SKILL.md` 的內容其實就是 Prompt，寫得越清楚，Agent 執行越精準。
*   **減少 Context**: 用 Skill 把細節藏起來，主對話視窗保持乾淨。

---
*整理自 YouTube 教程與 Medium 技術文章*

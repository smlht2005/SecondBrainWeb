# 測試知識庫文件

## 簡介
這是一個測試文件，用於驗證 Second Brain Web 系統是否正常運作。

## 功能測試

### Markdown 渲染
- ✅ 支援列表
- ✅ 支援 **粗體** 和 *斜體*
- ✅ 支援代碼塊

### 代碼高亮測試

```typescript
// TypeScript 範例
interface User {
  id: number;
  name: string;
}

const greet = (user: User): string => {
  return `Hello, ${user.name}!`;
};
```

```python
# Python 範例
def fibonacci(n: int) -> int:
    if n <= 1:
        return n
    return fibonacci(n-1) + fibonacci(n-2)
```

## 結論
如果你能看到這個文件並且格式正確，說明系統運作正常！🎉
